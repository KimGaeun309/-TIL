# App 제작 - 글목록 출력하기

파일이 추가되면 알아서 목록이 추가되도록 하자.

1. `fs.readdir()` 를 사용해 원하는 디렉토리의 파일 목록을 받는다.
2. `fs.readdir()` 의 매개변수로 들어가는 함수에 반복문을 이용해 `var list` 에 파일 목록에 맞추어 글머리 기호 목록을 저장하는 코드를 넣는다.
3. template 에서 글머리 목록 부분도 `${list}` 로 바꾸어준다.

**데이터가 바뀔 때 로직을 변경하는 것은 부끄러운 일.**
